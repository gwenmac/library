# library

To run locally:

Start the bootRun via gradle
in library-vue folder run `npm run dev`
Start library database in podman

Go to http://127.0.0.1:5173/

to deploy:

Go to the droplet web console
cd /opt/library/deploy
./deploy.sh
http://174.138.34.96/